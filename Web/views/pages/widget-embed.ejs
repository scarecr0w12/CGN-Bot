<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title><%= pageData.pageTitle %></title>
	<style>
	* {
		margin: 0;
		padding: 0;
		box-sizing: border-box;
	}
	body {
		font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, sans-serif;
		background: transparent;
	}
	.widget-container {
		padding: 16px;
		border-radius: 12px;
	}
	.widget-container.dark {
		background: #1a1a2e;
		color: #ffffff;
	}
	.widget-container.light {
		background: #ffffff;
		color: #1a1a2e;
		border: 1px solid #e0e0e0;
	}
	.widget-container.discord {
		background: #36393f;
		color: #ffffff;
	}
	.widget-header {
		display: flex;
		align-items: center;
		gap: 12px;
		margin-bottom: 16px;
	}
	.widget-icon {
		width: 48px;
		height: 48px;
		border-radius: 12px;
	}
	.widget-name {
		font-size: 18px;
		font-weight: 700;
	}
	.widget-stats {
		display: flex;
		gap: 24px;
	}
	.widget-stat {
		text-align: center;
	}
	.widget-stat-value {
		font-size: 24px;
		font-weight: 700;
		color: #14b8a6;
	}
	.widget-stat-label {
		font-size: 12px;
		opacity: 0.7;
	}
	.widget-footer {
		margin-top: 12px;
		font-size: 10px;
		opacity: 0.5;
		text-align: right;
	}
	.widget-footer a {
		color: inherit;
		text-decoration: none;
	}
	.widget-footer a:hover {
		text-decoration: underline;
	}
	</style>
</head>
<body>
	<div class="widget-container <%= pageData.theme %>">
		<div class="widget-header">
			<img src="<%= pageData.serverData.icon %>" alt="<%= pageData.serverData.name %>" class="widget-icon" onerror="this.style.display='none'">
			<div class="widget-name"><%= pageData.serverData.name %></div>
		</div>
		
		<% if (pageData.widgetType === 'stats') { %>
			<div class="widget-stats">
				<div class="widget-stat">
					<div class="widget-stat-value"><%= pageData.serverData.memberCount.toLocaleString() %></div>
					<div class="widget-stat-label">Members</div>
				</div>
				<div class="widget-stat">
					<div class="widget-stat-value"><%= pageData.serverData.messagesCount.toLocaleString() %></div>
					<div class="widget-stat-label">Messages Today</div>
				</div>
			</div>
		<% } %>
		
		<div class="widget-footer">
			<a href="https://skynetbot.com" target="_blank" rel="noopener">Powered by Skynet</a>
		</div>
	</div>
</body>
</html>
