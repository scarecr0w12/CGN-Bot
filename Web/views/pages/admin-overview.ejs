<!DOCTYPE html>
<html lang="en">
<head>
	<title><%= serverData.name %> Admin Console - Skynet</title>
	<%- include('../partials/head') %>
</head>
<body>
	<%- include('../partials/dashboard-header') %>

	<section class="section is-white">
		<div class="container">
			<div class="columns">
				<div id="menu" class="column is-one-quarter">
					<%- include('../partials/admin-menu') %>
				</div>
				<div class="column">
					<article class="message is-primary modern-card">
						<div class="message-header">
							<p style="display: flex; align-items: center; gap: 0.5rem;">
								<span class="icon"><i class="fa fa-dashboard"></i></span>
								Welcome to <strong><%= serverData.name %></strong>
							</p>
						</div>
						<div class="message-body">
							<p>
								The Skynet <strong>admin console</strong> gives you full access to all of the bot's <strong>settings</strong> for this server. Use the menu on the left to navigate between different sections. Below is an overview of your server's activity.
							</p>
						</div>
					</article>
					
					<!-- Modern Stats Grid -->
					<div class="stats-grid">
						<!-- Members -->
						<div class="stat-card">
							<div class="icon is-large has-text-primary" style="margin: 0 auto 0.75rem;">
								<i class="fa fa-users fa-2x"></i>
							</div>
							<p class="stat-value"><%= pageData.memberCount %></p>
							<p class="stat-label">Members</p>
						</div>
						
						<!-- Messages Today -->
						<div class="stat-card">
							<div class="icon is-large has-text-success" style="margin: 0 auto 0.75rem;">
								<i class="fa fa-comments fa-2x"></i>
							</div>
							<p class="stat-value"><%= pageData.messagesToday %></p>
							<p class="stat-label">Messages Today</p>
						</div>
						
						<% if (pageData.topCommand) { %>
						<!-- Top Command -->
						<div class="stat-card">
							<div class="icon is-large has-text-warning" style="margin: 0 auto 0.75rem;">
								<i class="fa fa-terminal fa-2x"></i>
							</div>
							<p class="stat-value" style="font-size: 1.5rem;"><%= pageData.topCommand %></p>
							<p class="stat-label">Top Command</p>
						</div>
						<% } %>
					</div>
					
					<!-- Server Info Cards -->
					<div class="columns">
						<!-- Server Owner -->
						<div class="column is-6">
							<div class="modern-card" style="padding: 1.25rem; display: flex; align-items: center; gap: 1rem;">
								<img src="<%= serverData.owner.avatar %>" alt="<%= serverData.owner.id %>" style="width: 48px; height: 48px; border-radius: 50%; border: 3px solid var(--color-border);">
								<div>
									<p class="stat-label">Server Owner</p>
									<p style="font-weight: 600; font-size: 1.1rem;">@<%= serverData.owner.username %></p>
								</div>
							</div>
						</div>
						
						<% if (pageData.topMember) { %>
						<!-- Top Member -->
						<div class="column is-6">
							<div class="modern-card" style="padding: 1.25rem; display: flex; align-items: center; gap: 1rem;">
								<div style="position: relative;">
									<img src="<%= pageData.topMember.avatar %>" alt="<%= pageData.topMember.id %>" style="width: 48px; height: 48px; border-radius: 50%; border: 3px solid var(--color-accent);">
									<span style="position: absolute; bottom: -2px; right: -2px; background: var(--color-accent); color: white; width: 20px; height: 20px; border-radius: 50%; display: flex; align-items: center; justify-content: center; font-size: 10px; border: 2px solid white;">
										<i class="fa fa-star"></i>
									</span>
								</div>
								<div>
									<p class="stat-label">Top Member</p>
									<p style="font-weight: 600; font-size: 1.1rem;">@<%= pageData.topMember.username %></p>
								</div>
							</div>
						</div>
						<% } %>
					</div>
					
					<% if (pageData.richestMember) { %>
					<div class="columns">
						<!-- Richest Member -->
						<div class="column is-6">
							<div class="modern-card" style="padding: 1.25rem; display: flex; align-items: center; gap: 1rem; background: linear-gradient(135deg, #fef3c7, #fde68a); border-color: #fcd34d;">
								<div style="position: relative;">
									<img src="<%= pageData.richestMember.avatar %>" alt="<%= pageData.richestMember.id %>" style="width: 48px; height: 48px; border-radius: 50%; border: 3px solid #f59e0b;">
									<span style="position: absolute; bottom: -2px; right: -2px; background: #f59e0b; color: white; width: 20px; height: 20px; border-radius: 50%; display: flex; align-items: center; justify-content: center; font-size: 10px; border: 2px solid white;">
										<i class="fa fa-diamond"></i>
									</span>
								</div>
								<div>
									<p class="stat-label" style="color: #92400e;">Richest Member</p>
									<p style="font-weight: 600; color: #78350f; font-size: 1.1rem;">@<%= pageData.richestMember.username %></p>
								</div>
							</div>
						</div>
					</div>
					<% } %>

					<!-- Server Actions -->
					<article class="message is-info modern-card" style="margin-top: 1.5rem;">
						<div class="message-header">
							<p style="display: flex; align-items: center; gap: 0.5rem;">
								<span class="icon"><i class="fa fa-cogs"></i></span>
								Server Actions
							</p>
						</div>
						<div class="message-body">
							<p style="margin-bottom: 1rem;">
								<strong>Scan Members</strong> - Index all server members into the database so they appear in user search on the Activity page.
							</p>
							<button id="scan-members-btn" class="button is-primary" onclick="SkynetUtil.scanServerMembers()">
								<span class="icon"><i class="fa fa-refresh"></i></span>
								<span>Scan Server Members</span>
							</button>
							<div id="scan-result" style="margin-top: 1rem; display: none;"></div>
						</div>
					</article>

					<script>
					SkynetUtil.scanServerMembers = async () => {
						const btn = document.getElementById('scan-members-btn');
						const resultDiv = document.getElementById('scan-result');
						btn.classList.add('is-loading');
						btn.disabled = true;
						resultDiv.style.display = 'none';

						try {
							const response = await fetch('/dashboard/<%= serverData.id %>/administration/scan-members', {
								method: 'POST',
								headers: { 'Content-Type': 'application/json' }
							});
							const data = await response.json();

							if (data.success) {
								resultDiv.innerHTML = '<div class="notification is-success">' +
									'<strong>Scan Complete!</strong><br>' +
									'Total Members: ' + data.totalMembers + '<br>' +
									'New Users Created: ' + data.created + '<br>' +
									'Usernames Updated: ' + data.updated + '<br>' +
									'Already Up-to-date: ' + data.skipped +
									'</div>';
							} else {
								resultDiv.innerHTML = '<div class="notification is-danger">Error: ' + (data.error || 'Unknown error') + '</div>';
							}
						} catch (err) {
							resultDiv.innerHTML = '<div class="notification is-danger">Error: ' + err.message + '</div>';
						}

						btn.classList.remove('is-loading');
						btn.disabled = false;
						resultDiv.style.display = 'block';
					};
					</script>
				</div>
			</div>
		</div>
	</section>

	<%- include('../partials/footer') %>
	<%- include('../partials/scroll-top-button') %>
</body>
</html>
