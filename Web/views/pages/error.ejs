<!DOCTYPE html>
<html lang="en">
<head>
	<title>Error | Skynet</title>
	<%- include('../partials/head') %>
	<style>
	.error-page {
		min-height: 100vh;
		display: flex;
		flex-direction: column;
		background: linear-gradient(135deg, #1a1a2e 0%, #16213e 50%, #0f3460 100%);
	}
	.error-hero {
		flex: 1;
		display: flex;
		align-items: center;
		justify-content: center;
		padding: 2rem;
	}
	.error-content {
		text-align: center;
		max-width: 550px;
	}
	.error-icon {
		width: 120px;
		height: 120px;
		background: rgba(255, 193, 7, 0.2);
		border-radius: 50%;
		display: flex;
		align-items: center;
		justify-content: center;
		margin: 0 auto 2rem;
	}
	.error-icon i {
		font-size: 3rem;
		color: #ffc107;
	}
	.error-title {
		font-size: 1.75rem;
		color: white;
		margin-bottom: 1rem;
		line-height: 1.3;
	}
	.error-message {
		color: rgba(255, 255, 255, 0.6);
		margin-bottom: 2rem;
		line-height: 1.6;
		font-size: 1rem;
	}
	.error-details {
		background: rgba(255, 255, 255, 0.05);
		border: 1px solid rgba(255, 255, 255, 0.1);
		border-radius: var(--radius-lg);
		padding: 1rem;
		margin-bottom: 2rem;
		text-align: left;
	}
	.error-details code {
		color: rgba(255, 255, 255, 0.7);
		font-size: 0.85rem;
		word-break: break-word;
	}
	.error-actions {
		display: flex;
		gap: 1rem;
		justify-content: center;
		flex-wrap: wrap;
	}
	.error-actions .button {
		border-radius: var(--radius-md);
		min-width: 140px;
	}
	.error-brand {
		padding: 1.5rem;
		text-align: center;
	}
	.error-brand img {
		width: 48px;
		height: 48px;
		border-radius: 50%;
		margin-bottom: 0.5rem;
	}
	.error-brand-name {
		color: rgba(255, 255, 255, 0.7);
		font-weight: 600;
		font-size: 0.9rem;
	}
	</style>
</head>
<body>
	<div class="error-page">
		<div class="error-brand">
			<a href="/">
				<img src="/static/img/icon.png" alt="Skynet">
				<div class="error-brand-name">Skynet</div>
			</a>
		</div>
		<div class="error-hero">
			<div class="error-content">
				<div class="error-icon">
					<i class="fa fa-exclamation-circle"></i>
				</div>
				<h1 class="error-title"><%- error_line %></h1>
				<% if (typeof error_text !== 'undefined' && error_text) { %>
				<div class="error-details">
					<code><%- error_text %></code>
				</div>
				<% } else { %>
				<p class="error-message">
					Something went wrong while processing your request. 
					Please try again or contact support if the problem persists.
				</p>
				<% } %>
				<div class="error-actions">
					<a class="button is-light" href="javascript:window.history.back();">
						<span class="icon"><i class="fa fa-arrow-left"></i></span>
						<span>Go Back</span>
					</a>
					<a class="button is-warning" href="/">
						<span class="icon"><i class="fa fa-home"></i></span>
						<span>Home</span>
					</a>
				</div>
			</div>
		</div>
	</div>
</body>
</html>
