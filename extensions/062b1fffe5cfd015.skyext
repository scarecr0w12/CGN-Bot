const message = require('message');
const embed = require('embed');

const target = message.mentions[0] || message.author;
const avatar = target.avatarURL ? target.avatarURL({ size: 512 }) : '';

const imageUrl = 'https://api.popcat.xyz/drip?image=' + encodeURIComponent(avatar);

message.reply({
	embeds: [embed.create({
		title: '🧥 Drip Check',
		description: target.username + ' got that drip! 💧',
		image: { url: imageUrl },
		color: embed.colors.GOLD,
	})],
});