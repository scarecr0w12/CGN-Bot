const message = require('message');
const embed = require('embed');

const target = message.mentions[0] || message.author;
const avatar = target.avatarURL ? target.avatarURL({ size: 512 }) : '';

const imageUrl = 'https://api.popcat.xyz/ad?image=' + encodeURIComponent(avatar);

message.reply({
	embeds: [embed.create({
		title: '📱 Mobile Ad',
		description: 'Is ' + target.username + ' your card?',
		image: { url: imageUrl },
		color: embed.colors.DEFAULT,
	})],
});