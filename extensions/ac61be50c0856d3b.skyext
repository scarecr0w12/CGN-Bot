const message = require('message');
const embed = require('embed');

const target = message.mentions[0] || message.author;
const avatar = target.avatarURL ? target.avatarURL({ size: 512 }) : '';

const imageUrl = 'https://some-random-api.com/canvas/overlay/passed?avatar=' + encodeURIComponent(avatar);

message.reply({
	embeds: [embed.create({
		title: '✅ MISSION PASSED',
		description: 'Respect+ for ' + target.username,
		image: { url: imageUrl },
		color: embed.colors.SUCCESS,
	})],
});