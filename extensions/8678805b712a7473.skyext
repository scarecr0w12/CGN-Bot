const message = require('message');
const embed = require('embed');

const target = message.mentions[0] || message.author;
const avatar = target.avatarURL ? target.avatarURL({ size: 512 }) : '';

const imageUrl = 'https://some-random-api.com/canvas/filter/invert?avatar=' + encodeURIComponent(avatar);

message.reply({
	embeds: [embed.create({
		title: '🔄 Inverted',
		description: target.username + '\'s avatar with inverted colors',
		image: { url: imageUrl },
		color: embed.colors.PURPLE,
	})],
});