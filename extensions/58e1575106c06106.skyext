const message = require('message');
const embed = require('embed');

const target = message.mentions[0] || message.author;
const avatar = target.avatarURL ? target.avatarURL({ size: 512 }) : '';

const imageUrl = 'https://api.popcat.xyz/clown?image=' + encodeURIComponent(avatar);

message.reply({
	embeds: [embed.create({
		title: '🤡 Clown',
		description: target.username + ' is a certified clown!',
		image: { url: imageUrl },
		color: embed.colors.ERROR,
	})],
});