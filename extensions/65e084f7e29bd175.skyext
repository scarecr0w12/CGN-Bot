const message = require('message');
const embed = require('embed');

const target = message.mentions[0] || message.author;
const avatar = target.avatarURL ? target.avatarURL({ size: 512 }) : '';

const imageUrl = 'https://some-random-api.com/canvas/misc/beautiful?avatar=' + encodeURIComponent(avatar);

message.reply({
	embeds: [embed.create({
		title: '✨ Beautiful',
		description: 'Oh this? This is beautiful.',
		image: { url: imageUrl },
		color: embed.colors.PINK,
	})],
});