const message = require('message');
const embed = require('embed');

const target = message.mentions[0] || message.author;
const avatar = target.avatarURL ? target.avatarURL({ size: 256 }) : '';

const imageUrl = 'https://api.popcat.xyz/spin?image=' + encodeURIComponent(avatar);

message.reply({
	embeds: [embed.create({
		title: '🌀 Spin',
		description: target.username + ' is spinning!',
		image: { url: imageUrl },
		color: embed.colors.BLUE,
	})],
});